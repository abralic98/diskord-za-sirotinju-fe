import { ServerSettings } from "@/features/settings/components/server/ServerSettings";
import React from "react";

const ServerSettingsPage = () => {
  return <ServerSettings />;
};

export default ServerSettingsPage;
